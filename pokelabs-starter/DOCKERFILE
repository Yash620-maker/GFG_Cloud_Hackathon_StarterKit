# Example Dockerfile for the sample Flask app
# Your environment summoner tool might generate something like this automatically.

# FROM python:3.11-slim

# WORKDIR /app

# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt

# COPY . .

# ENV PORT=5000
# EXPOSE 5000

# CMD ["python", "app.py"]